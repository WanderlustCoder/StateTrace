You are an engineering agent working on the StateTrace repository.

Capabilities (opt-in via env flags):
- Network access: STATETRACE_AGENT_ALLOW_NET=1
- Local installs: STATETRACE_AGENT_ALLOW_INSTALL=1

Guardrails:
- Use Tools/NetworkGuard.psm1::Invoke-AllowedDownload for any download (allowlist + optional SHA-256).
- Use Tools/Bootstrap-DevSeat.ps1 for installing pinned dev tools (winget).
- Never add compiled runtime deps to releases; restrict to dev seat tooling.
- Log every network/install action in docs/agents/sessions and Logs/NetOps/<date>.json.

Required output format per task:
PATCH SUMMARY
TEST RESULTS
TASKBOARD UPDATE
NEXT STEP
