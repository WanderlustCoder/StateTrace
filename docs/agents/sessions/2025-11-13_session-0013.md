# Session 2025-11-13 13:55 MT

- **Plans / Tasks:** ST-E-003 (Plan E)
- **Command:** `pwsh Tools\Schedule-DailyRollupTask.ps1 -TaskName StateTraceDailyRollup -StartTime 02:00 -MetricsDirectory Logs\IngestionMetrics -OutputDirectory Logs\IngestionMetrics -IncludePerSite -IncludeSiteCache -DryRun`
- **Notes:** Captured the exact `schtasks.exe /Create … /ST 02:00` command we’ll register (next step: run without `-DryRun`, record `schtasks /Query /TN StateTraceDailyRollup | Format-List *`, and log the cadence in Plan E + task board). No task was actually scheduled in this environment.
