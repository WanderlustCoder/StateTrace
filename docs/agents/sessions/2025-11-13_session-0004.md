# Agent Session Log

- **Date:** 2025-11-13
- **Role:** Security / Automation (Plan F)
- **Task board card:** ST-F-003 – NetOps session log enforcement

## Plan (3-6 bullets)
- Build a lint harness that fails when online-mode sessions skip NetOps/reset evidence or omit references in the session log.
- Wire the harness into `Tools/Invoke-AllChecks.ps1` so CI/agents can run it with a single flag.
- Update Plan F, Task Board/CSV, automation docs, and session logs with the new workflow.

## Changes
- Added `Tools/Test-NetOpsEvidence.ps1` (verifies `Logs/NetOps/*.json`, reset logs, and optional session-log references within a configurable time window).
- Updated `Tools/Invoke-AllChecks.ps1` with `-SkipNetOpsLint`, `-RequireNetOpsEvidence`, and `-NetOpsSessionLogPath` switches so NetOps evidence can be enforced during routine check runs.
- Documented the workflow in Plan F, the plan index, Task Board/CSV rows, automation matrix, runbook, and checklist.
- Logged ST-F-003 completion on the Task Board and plan timeline.

## Validation
- Not run (lint script relies on local NetOps directories; no online-mode activity occurred this session).

## Outcome
- Status: ST-F-003 is now Done (NetOps lint script + AllChecks hook in place).
- Follow-ups: Ensure future online-mode sessions run `Tools\Test-NetOpsEvidence.ps1 -RequireEvidence` (or `Tools\Invoke-AllChecks.ps1 -RequireNetOpsEvidence`) before handing off work.
