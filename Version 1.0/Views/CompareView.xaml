<UserControl xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <UserControl.Resources>
    <!-- Local style for editable combo boxes specific to this view -->
    <Style x:Key="SlimCombo" TargetType="ComboBox" BasedOn="{StaticResource ToolbarComboBoxStyle}">
      <Setter Property="Margin" Value="0,0,0,6"/>
      <Setter Property="MinWidth" Value="160"/>
      <Setter Property="IsEditable" Value="True"/>
      <Setter Property="IsTextSearchEnabled" Value="True"/>
    </Style>
  </UserControl.Resources>

  <Grid Background="{DynamicResource Theme.Surface.Secondary}">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>  
      <RowDefinition Height="*"/>     
    </Grid.RowDefinitions>

    <DockPanel Grid.Row="0" LastChildFill="False" Background="{DynamicResource Theme.Toolbar.Background}" Height="34">
      <TextBlock Text="Compare Configs"
                 DockPanel.Dock="Left"
                 VerticalAlignment="Center"
                 Margin="8,0,0,0"
                 Foreground="{DynamicResource Theme.Toolbar.Text}"
                 FontWeight="Bold"/>
      <Button Name="CloseCompareButton"
              DockPanel.Dock="Right"
              Content="X"
              Width="26"
              Height="22"
              VerticalAlignment="Center"
              Margin="0,0,6,0"
              Style="{DynamicResource IconButtonStyle}"
              ToolTip="Close compare sidebar"/>
    </DockPanel>

    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
      <Grid Margin="8">
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto"/>  
          <RowDefinition Height="8"/>     
          <RowDefinition Height="Auto"/>  
          <RowDefinition Height="8"/>     
          <RowDefinition Height="Auto"/>  
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
          </Grid.ColumnDefinitions>

          <StackPanel Grid.Column="0" Orientation="Vertical" Margin="4,0,8,0">
            <TextBlock Text="Switch 1" Style="{DynamicResource FieldLabelStyle}"/>
            <ComboBox Name="Switch1Dropdown" Style="{StaticResource SlimCombo}"/>

            <TextBlock Text="Port 1" Style="{DynamicResource FieldLabelStyle}"/>
            <ComboBox Name="Port1Dropdown" Style="{StaticResource SlimCombo}"/>
          </StackPanel>

          <StackPanel Grid.Column="1" Orientation="Vertical" Margin="8,0,4,0">
            <TextBlock Text="Switch 2" Style="{DynamicResource FieldLabelStyle}"/>
            <ComboBox Name="Switch2Dropdown" Style="{StaticResource SlimCombo}"/>

            <TextBlock Text="Port 2" Style="{DynamicResource FieldLabelStyle}"/>
            <ComboBox Name="Port2Dropdown" Style="{StaticResource SlimCombo}"/>
          </StackPanel>
        </Grid>

        <Border Grid.Row="1" Height="1" Background="{DynamicResource Theme.Surface.Border}" Margin="0,4,0,4" Opacity="0.5"/>

        <Grid Grid.Row="2">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
          </Grid.ColumnDefinitions>

          <StackPanel Grid.Column="0" Orientation="Vertical" Margin="4,0,8,0">
            <TextBlock Name="AuthTemplate1Text"
                       Text=""
                       FontSize="12"
                       FontWeight="Bold"
                       Foreground="{DynamicResource Theme.Text.Primary}"
                       Margin="0,0,0,2"/>
            <TextBlock Text="Config 1" Style="{DynamicResource SectionTitleStyle}"/>

            <Border Style="{DynamicResource CompactCardStyle}">

                <TextBox Name="Config1Box"
                         TextWrapping="Wrap"
                         AcceptsReturn="True"
                         IsReadOnly="True"
                         Background="{DynamicResource Theme.Input.Background}"
                         BorderThickness="0"
                         FontFamily="Consolas"
                         FontSize="12"
                         ScrollViewer.HorizontalScrollBarVisibility="Auto"
                         ScrollViewer.VerticalScrollBarVisibility="Auto"
                         MaxHeight="300" />
            </Border>
          </StackPanel>

          <StackPanel Grid.Column="1" Orientation="Vertical" Margin="8,0,4,0">
            <TextBlock Name="AuthTemplate2Text"
                       Text=""
                       FontSize="12"
                       FontWeight="Bold"
                       Foreground="{DynamicResource Theme.Text.Primary}"
                       Margin="0,0,0,2"/>
            <TextBlock Text="Config 2" Style="{DynamicResource SectionTitleStyle}"/>

            <Border Style="{DynamicResource CompactCardStyle}">
                <TextBox Name="Config2Box"
                         TextWrapping="Wrap"
                         AcceptsReturn="True"
                         IsReadOnly="True"
                         Background="{DynamicResource Theme.Input.Background}"
                         BorderThickness="0"
                         FontFamily="Consolas"
                         FontSize="12"
                         ScrollViewer.HorizontalScrollBarVisibility="Auto"
                         ScrollViewer.VerticalScrollBarVisibility="Auto"
                         MaxHeight="300" />
            </Border>
          </StackPanel>
        </Grid>

        <Border Grid.Row="3" Height="1" Background="{DynamicResource Theme.Surface.Border}" Margin="0,4,0,4" Opacity="0.5"/>

        <Grid Grid.Row="4">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
          </Grid.ColumnDefinitions>

          <StackPanel Grid.Column="0" Orientation="Vertical" Margin="4,0,8,0">
            <TextBlock Text="Commands only in Config 1" Style="{DynamicResource SectionTitleStyle}"/>
            <Border Style="{DynamicResource CompactCardStyle}">
                <TextBox Name="Diff1Box"
                         TextWrapping="Wrap"
                         AcceptsReturn="True"
                         IsReadOnly="True"
                         Background="{DynamicResource Theme.Input.Background}"
                         BorderThickness="0"
                         FontFamily="Consolas"
                         FontSize="12"
                         Foreground="{DynamicResource Theme.Status.Danger}"
                         ScrollViewer.HorizontalScrollBarVisibility="Auto"
                         ScrollViewer.VerticalScrollBarVisibility="Auto"
                         MaxHeight="300" />
            </Border>
          </StackPanel>

          <StackPanel Grid.Column="1" Orientation="Vertical" Margin="8,0,4,0">
            <TextBlock Text="Commands only in Config 2" Style="{DynamicResource SectionTitleStyle}"/>
            <Border Style="{DynamicResource CompactCardStyle}">
                <TextBox Name="Diff2Box"
                         TextWrapping="Wrap"
                         AcceptsReturn="True"
                         IsReadOnly="True"
                         Background="{DynamicResource Theme.Input.Background}"
                         BorderThickness="0"
                         FontFamily="Consolas"
                         FontSize="12"
                         Foreground="{DynamicResource Theme.Status.Danger}"
                         ScrollViewer.HorizontalScrollBarVisibility="Auto"
                         ScrollViewer.VerticalScrollBarVisibility="Auto"
                         MaxHeight="300" />
            </Border>
          </StackPanel>
        </Grid>

      </Grid>
    </ScrollViewer>
  </Grid>
</UserControl>
