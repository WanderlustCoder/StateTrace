<UserControl xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <UserControl.Resources>

    <Style x:Key="StatusTextStyle" TargetType="TextBlock">
      <Setter Property="Foreground" Value="{DynamicResource Theme.Text.Primary}"/>
      <Style.Triggers>
        <DataTrigger Binding="{Binding Status}" Value="connected">
          <Setter Property="Foreground" Value="{DynamicResource Theme.Status.Success}"/>
        </DataTrigger>
        <DataTrigger Binding="{Binding Status}" Value="notconnect">
          <Setter Property="Foreground" Value="{DynamicResource Theme.Status.Danger}"/>
        </DataTrigger>
        <DataTrigger Binding="{Binding Status}" Value="Up">
          <Setter Property="Foreground" Value="{DynamicResource Theme.Status.Success}"/>
        </DataTrigger>
        <DataTrigger Binding="{Binding Status}" Value="Down">
          <Setter Property="Foreground" Value="{DynamicResource Theme.Status.Danger}"/>
        </DataTrigger>
      </Style.Triggers>
    </Style>

    <Style x:Key="AuthStateTextStyle" TargetType="TextBlock">
      <Setter Property="Foreground" Value="{DynamicResource Theme.Text.Primary}"/>
      <Style.Triggers>
        <DataTrigger Binding="{Binding AuthState}" Value="Authorized">
          <Setter Property="Foreground" Value="{DynamicResource Theme.Status.Success}"/>
        </DataTrigger>
        <DataTrigger Binding="{Binding AuthState}" Value="Unauthorized">
          <Setter Property="Foreground" Value="{DynamicResource Theme.Status.Danger}"/>
        </DataTrigger>
      </Style.Triggers>
    </Style>

    <Style x:Key="DuplexTextStyle" TargetType="TextBlock">
      <Setter Property="Foreground" Value="{DynamicResource Theme.Text.Primary}"/>
      <Style.Triggers>
        <DataTrigger Binding="{Binding Duplex}" Value="full">
          <Setter Property="Foreground" Value="{DynamicResource Theme.Status.Success}"/>
        </DataTrigger>
        <DataTrigger Binding="{Binding Duplex}" Value="half">
          <Setter Property="Foreground" Value="{DynamicResource Theme.Status.Warning}"/>
        </DataTrigger>
        <DataTrigger Binding="{Binding Duplex}" Value="a-full">
          <Setter Property="Foreground" Value="{DynamicResource Theme.Status.Info}"/>
        </DataTrigger>
      </Style.Triggers>
    </Style>

    <Style x:Key="PortTextStyle" TargetType="TextBlock">
      <Setter Property="Foreground" Value="{DynamicResource Theme.Status.Neutral}"/>
      <Style.Triggers>
        <DataTrigger Binding="{Binding PortColor}" Value="Green">
          <Setter Property="Foreground" Value="{DynamicResource Theme.Template.Green}"/>
        </DataTrigger>
        <DataTrigger Binding="{Binding PortColor}" Value="Blue">
          <Setter Property="Foreground" Value="{DynamicResource Theme.Template.Blue}"/>
        </DataTrigger>
        <DataTrigger Binding="{Binding PortColor}" Value="Purple">
          <Setter Property="Foreground" Value="{DynamicResource Theme.Template.Purple}"/>
        </DataTrigger>
        <DataTrigger Binding="{Binding PortColor}" Value="Red">
          <Setter Property="Foreground" Value="{DynamicResource Theme.Template.Red}"/>
        </DataTrigger>
      </Style.Triggers>
    </Style>
  </UserControl.Resources>

  <Grid Background="{DynamicResource Theme.Surface.Primary}">
    <Grid.RowDefinitions>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*"/>
    </Grid.ColumnDefinitions>

    <Grid Grid.Row="0" Grid.Column="0" Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,5">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
          </Grid.ColumnDefinitions>

          <Grid Grid.Column="0" Margin="0,0,15,0">
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto"/>
              <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="Auto"/>
              <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Name="CompareButton"
                    Content="Compare Configs"
                    Width="120"
                    Margin="0,0,10,5"
                    Style="{DynamicResource PrimaryButtonStyle}"
                    Grid.Row="0" Grid.Column="0"/>
            <Button Name="CopyDetailsButton"
                    Content="Copy Details"
                    Width="120"
                    Margin="0,0,10,0"
                    Style="{DynamicResource PrimaryButtonStyle}"
                    ToolTip="Copy selected interfaces with all details"
                    Grid.Row="1" Grid.Column="0"/>
            <Button Name="ConfigureButton"
                    Content="Configure"
                    Width="120"
                    Margin="0,0,0,5"
                    Style="{DynamicResource PrimaryButtonStyle}"
                    Grid.Row="0" Grid.Column="1"/>
            <ComboBox Name="ConfigOptionsDropdown"
                      Width="120"
                      Margin="0"
                      Grid.Row="1" Grid.Column="1" Style="{DynamicResource ToolbarComboBoxStyle}"/>
          </Grid>

          <StackPanel Grid.Column="1" Orientation="Vertical">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
              <StackPanel Margin="0,0,15,0">
                <TextBlock Text="Hostname:" FontSize="12" Foreground="{DynamicResource Theme.Text.Inverse}"/>
                <TextBox Name="HostnameBox" IsReadOnly="True" Width="130" Background="{DynamicResource Theme.Input.Background}" Foreground="{DynamicResource Theme.Input.Text}" BorderBrush="{DynamicResource Theme.Input.Border}" BorderThickness="1"/>
              </StackPanel>
              <StackPanel Margin="0,0,15,0">
                <TextBlock Text="Make:" FontSize="12" Foreground="{DynamicResource Theme.Text.Inverse}"/>
                <TextBox Name="MakeBox" IsReadOnly="True" Width="90" Background="{DynamicResource Theme.Input.Background}" Foreground="{DynamicResource Theme.Input.Text}" BorderBrush="{DynamicResource Theme.Input.Border}" BorderThickness="1"/>
              </StackPanel>
              <StackPanel Margin="0,0,15,0">
                <TextBlock Text="Model:" FontSize="12" Foreground="{DynamicResource Theme.Text.Inverse}"/>
                <TextBox Name="ModelBox" IsReadOnly="True" Width="100" Background="{DynamicResource Theme.Input.Background}" Foreground="{DynamicResource Theme.Input.Text}" BorderBrush="{DynamicResource Theme.Input.Border}" BorderThickness="1"/>
              </StackPanel>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
              <StackPanel Margin="0,0,15,0">
                <TextBlock Text="Uptime:" FontSize="12" Foreground="{DynamicResource Theme.Text.Inverse}"/>
                <TextBox Name="UptimeBox" IsReadOnly="True" Width="140" Background="{DynamicResource Theme.Input.Background}" Foreground="{DynamicResource Theme.Input.Text}" BorderBrush="{DynamicResource Theme.Input.Border}" BorderThickness="1"/>
              </StackPanel>
              <StackPanel Margin="0,0,15,0">
                <TextBlock Text="Ports:" FontSize="12" Foreground="{DynamicResource Theme.Text.Inverse}"/>
                <TextBox Name="PortCountBox" IsReadOnly="True" Width="50" Background="{DynamicResource Theme.Input.Background}" Foreground="{DynamicResource Theme.Input.Text}" BorderBrush="{DynamicResource Theme.Input.Border}" BorderThickness="1"/>
              </StackPanel>
              <StackPanel Margin="0,0,15,0">
                <TextBlock Text="AuthDefaultVLAN:" FontSize="12" Foreground="{DynamicResource Theme.Text.Inverse}"/>
                <TextBox Name="AuthDefaultVLANBox" IsReadOnly="True" Width="130" Background="{DynamicResource Theme.Input.Background}" Foreground="{DynamicResource Theme.Input.Text}" BorderBrush="{DynamicResource Theme.Input.Border}" BorderThickness="1"/>
              </StackPanel>
              <StackPanel Margin="0,0,15,0">
                <TextBlock Text="Building:" FontSize="12" Foreground="{DynamicResource Theme.Text.Inverse}"/>
                <TextBox Name="BuildingBox" IsReadOnly="True" Width="60" Background="{DynamicResource Theme.Input.Background}" Foreground="{DynamicResource Theme.Input.Text}" BorderBrush="{DynamicResource Theme.Input.Border}" BorderThickness="1"/>
              </StackPanel>
              <StackPanel Margin="0,0,15,0">
                <TextBlock Text="Room:" FontSize="12" Foreground="{DynamicResource Theme.Text.Inverse}"/>
                <TextBox Name="RoomBox" IsReadOnly="True" Width="40" Background="{DynamicResource Theme.Input.Background}" Foreground="{DynamicResource Theme.Input.Text}" BorderBrush="{DynamicResource Theme.Input.Border}" BorderThickness="1"/>
              </StackPanel>
            </StackPanel>
          </StackPanel>
        </Grid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,5">
            <TextBox Name="FilterBox"
                   Width="160"
                   Background="{DynamicResource Theme.Input.Background}" Foreground="{DynamicResource Theme.Input.Text}"
                   BorderBrush="{DynamicResource Theme.Input.Border}"
                   BorderThickness="1"
                   ToolTip="Type to filter interfaces..."
                   Margin="0,0,5,0"/>
            <Button Name="ClearFilterButton"
                  Content="X"
                  Width="30"
                  Height="23"
                  VerticalAlignment="Center"
                  ToolTip="Clear filter"/>
        </StackPanel>

        <DataGrid Name="InterfacesGrid"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  SelectionMode="Extended"
                  SelectionUnit="CellOrRowHeader"
                  Margin="5,0,5,5"
                  CanUserAddRows="False"
                  IsReadOnly="False"
                  Style="{DynamicResource PrimaryDataGridStyle}"
                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                  ScrollViewer.HorizontalScrollBarVisibility="Auto"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling"
                  EnableRowVirtualization="True"
                  EnableColumnVirtualization="True"
                  ScrollViewer.CanContentScroll="True"
                  VirtualizingPanel.ScrollUnit="Pixel">
          <DataGrid.RowStyle>
            <DynamicResource ResourceKey="PrimaryDataGridRowStyle"/>
          </DataGrid.RowStyle>
          <DataGrid.Columns>

            <DataGridCheckBoxColumn Width="30"
                                    Header=""
                                    IsThreeState="False">
              <DataGridCheckBoxColumn.Binding>
                <Binding Path="IsSelected" Mode="TwoWay" UpdateSourceTrigger="PropertyChanged"/>
              </DataGridCheckBoxColumn.Binding>
              <DataGridCheckBoxColumn.ElementStyle>
                <Style TargetType="CheckBox">
                  <Setter Property="HorizontalAlignment" Value="Center"/>
                  <Setter Property="VerticalAlignment"   Value="Center"/>
                  <Setter Property="Focusable"           Value="False"/>
                </Style>
              </DataGridCheckBoxColumn.ElementStyle>
              <DataGridCheckBoxColumn.EditingElementStyle>
                <Style TargetType="CheckBox">
                  <Setter Property="HorizontalAlignment" Value="Center"/>
                  <Setter Property="VerticalAlignment"   Value="Center"/>
                  <Setter Property="Focusable"           Value="False"/>
                </Style>
              </DataGridCheckBoxColumn.EditingElementStyle>
            </DataGridCheckBoxColumn>

            <DataGridTemplateColumn Header="Port">
              <DataGridTemplateColumn.CellTemplate>
                <DataTemplate>
                  <TextBlock Text="{Binding Port}"
                             Style="{StaticResource PortTextStyle}">
                    <TextBlock.ToolTip>
                      <ToolTip Content="{Binding ToolTip}"/>
                    </TextBlock.ToolTip>
                  </TextBlock>
                </DataTemplate>
              </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>

            <DataGridTextColumn Header="Name"
                                 Binding="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

            <DataGridTemplateColumn Header="Status">
              <DataGridTemplateColumn.CellTemplate>
                <DataTemplate>
                  <TextBlock Text="{Binding Status}"
                             Style="{StaticResource StatusTextStyle}"/>
                </DataTemplate>
              </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>

            <DataGridTextColumn Header="VLAN"
                                 Binding="{Binding VLAN, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

            <DataGridTextColumn Header="Duplex" Binding="{Binding Duplex}">
              <DataGridTextColumn.ElementStyle>
                <StaticResource ResourceKey="DuplexTextStyle"/>
              </DataGridTextColumn.ElementStyle>
            </DataGridTextColumn>

            <DataGridTextColumn Header="Speed" Binding="{Binding Speed}"/>

            <DataGridTextColumn Header="Type" Binding="{Binding Type}"/>

            <DataGridTextColumn Header="LearnedMACs" Binding="{Binding LearnedMACs}" Width="160">
              <DataGridTextColumn.ElementStyle>
                <Style TargetType="TextBlock">
                  <Setter Property="TextWrapping" Value="NoWrap"/>
                  <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                </Style>
              </DataGridTextColumn.ElementStyle>
            </DataGridTextColumn>

            <DataGridTextColumn Header="AuthState" Binding="{Binding AuthState}">
              <DataGridTextColumn.ElementStyle>
                <StaticResource ResourceKey="AuthStateTextStyle"/>
              </DataGridTextColumn.ElementStyle>
            </DataGridTextColumn>

            <DataGridTextColumn Header="AuthMode" Binding="{Binding AuthMode}"/>

            <DataGridTextColumn Header="AuthClientMAC" Binding="{Binding AuthClientMAC}"/>
          </DataGrid.Columns>
        </DataGrid>
    </Grid>
  </Grid>
</UserControl>
