<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Quick Navigation"
        Width="400" Height="350"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        ResizeMode="NoResize">

  <Border Background="{DynamicResource Theme.Surface.Primary}"
          BorderBrush="{DynamicResource Theme.Border.Primary}"
          BorderThickness="1" CornerRadius="8"
          Padding="10">
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
      </Grid.RowDefinitions>

      <!-- Header -->
      <TextBlock Grid.Row="0" Text="Quick Navigation (Ctrl+J)"
                 FontWeight="SemiBold" FontSize="14"
                 Foreground="{DynamicResource Theme.Text.Primary}"
                 Margin="0,0,0,10"/>

      <!-- Search box -->
      <TextBox Name="SearchBox" Grid.Row="1"
               Padding="8,6"
               Margin="0,0,0,10"
               Background="{DynamicResource Theme.Surface.Secondary}"
               Foreground="{DynamicResource Theme.Text.Primary}"
               BorderBrush="{DynamicResource Theme.Border.Primary}"
               BorderThickness="1"/>

      <!-- Navigation list -->
      <ListBox Name="NavigationList" Grid.Row="2"
               Background="{DynamicResource Theme.Surface.Secondary}"
               Foreground="{DynamicResource Theme.Text.Primary}"
               BorderBrush="{DynamicResource Theme.Border.Primary}"
               BorderThickness="1"
               ScrollViewer.HorizontalScrollBarVisibility="Disabled"
               VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling"
               ScrollViewer.CanContentScroll="True">
        <ListBox.ItemTemplate>
          <DataTemplate>
            <StackPanel Orientation="Horizontal" Margin="4,2">
              <TextBlock Text="{Binding Shortcut}" Width="50" FontFamily="Consolas"
                         Foreground="{DynamicResource Theme.Text.Secondary}"/>
              <TextBlock Text="{Binding Display}" Foreground="{DynamicResource Theme.Text.Primary}"/>
            </StackPanel>
          </DataTemplate>
        </ListBox.ItemTemplate>
        <ListBox.ItemContainerStyle>
          <Style TargetType="ListBoxItem">
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource Theme.Text.Primary}"/>
            <Style.Triggers>
              <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{DynamicResource Theme.Icon.Accent}"/>
                <Setter Property="Foreground" Value="{DynamicResource Theme.Button.Primary.Text}"/>
              </Trigger>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource Theme.Surface.Primary}"/>
              </Trigger>
            </Style.Triggers>
          </Style>
        </ListBox.ItemContainerStyle>
      </ListBox>
    </Grid>
  </Border>
</Window>
